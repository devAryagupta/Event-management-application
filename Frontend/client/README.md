# Frontend (React + Vite)

For live demo URLs, login credentials, and setup instructions, see the main project README:

[../../README.md](../../README.md)

## Local run

```bash
npm install
npm run dev
```

App: `http://localhost:5173`  
API proxy: `/api` → `http://127.0.0.1:3000`